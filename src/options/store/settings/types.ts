import * as actionTypes from "./action-types";

export const NAMESPACE = "settings";

export const GET_SETTINGS = `${NAMESPACE}/${actionTypes.GET_SETTINGS}`;
export const UPDATE_SETTINGS = `${NAMESPACE}/${actionTypes.UPDATE_SETTINGS}`;
